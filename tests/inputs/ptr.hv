
impure fn set_value(i32 *x) -> void {
    store x as<i32>(5);
}

impure fn load_value(i32 *x) -> i32 {
    load x
}

pub impure fn sut() -> i32 {
    let i32 x = as<i32>(0);
    set_value(ref x);
    load_value(ref x)
}
