state i32 calls = 0;

impure fn rhs() -> i32 {
    calls = calls + 10;
    1
}

impure fn and() -> i32 {
    as<i32>(0 && rhs())
}

impure fn or() -> i32 {
    as<i32>(1 || rhs())
}

pub impure fn sut() -> i32 {
    and() + or()
}
