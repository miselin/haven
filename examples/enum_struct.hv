
type Inner = struct {
    i32 x;
};

type Wrapped = enum {
    Inner(Inner),
    Error
};

pub fn main() -> i32 {
    let Inner inner = { as i32 5, };
    let foo = Wrapped::Inner(inner);
    match foo {
        Inner(x) => x.x
        _ => 1
    }
}
