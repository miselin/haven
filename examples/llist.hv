type node = struct {
    i32 value;
    node *next;
};

pub impure fn main() -> i32 {
    let node tail = { as i32 1, nil };
    let node head = { as i32 0, ref tail };

    let p = load (head.next);
    p.value
}
